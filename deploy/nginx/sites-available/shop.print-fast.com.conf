# Generated by scripts/render-nginx.mjs from deploy/apps.json — do not edit by hand.
# Shop website — https://shop.print-fast.com
#
# Certbot rewrites this file when it installs TLS (adding the :443 server block
# and the HTTP->HTTPS redirect), so deploys never overwrite an existing copy.
# Routing lives in /etc/nginx/snippets/pf-print-fast-shop.conf and is refreshed every deploy.

server {
    listen 80;
    listen [::]:80;
    server_name shop.print-fast.com;

    server_tokens off;

    access_log /var/log/nginx/print-fast-shop.access.log;
    error_log  /var/log/nginx/print-fast-shop.error.log;

    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    include /etc/nginx/snippets/pf-print-fast-shop.conf;
}
