---
import '@styles/global.css';
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';
import SEO, { type Props as SEOProps } from '@components/SEO.astro';
import AccessibilityDrawer from '@components/AccessibilityDrawer.astro';
import { SITE } from '@data/site';

export interface Props extends SEOProps {}

const seoProps = Astro.props;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />

    <!-- Set theme BEFORE first paint to prevent FOUC flash -->
    <script is:inline>
      (function () {
        try {
          var stored = localStorage.getItem('pf-theme');
          var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
          var theme = stored || (prefersDark ? 'dark' : 'light');
          if (theme === 'dark') document.documentElement.classList.add('dark');
        } catch (e) {}
      })();
    </script>

    <meta name="theme-color" content="#6FD12E" media="(prefers-color-scheme: light)" />
    <meta name="theme-color" content="#16181f" media="(prefers-color-scheme: dark)" />
    <meta name="color-scheme" content="light dark" />

    <!-- Performance hints -->
    <link rel="dns-prefetch" href="https://dqj17tese79do.cloudfront.net" />
    <link rel="preconnect" href="https://dqj17tese79do.cloudfront.net" crossorigin />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="preload" as="image" href={SITE.logoLight} />
    <link rel="preload" as="image" href={SITE.logoDark} />

    <!-- Favicons (always use the color/light logo for OS UI) -->
    <link rel="icon" type="image/png" href={SITE.logoLight} />
    <link rel="shortcut icon" href={SITE.logoLight} />
    <link rel="apple-touch-icon" href={SITE.logoLight} />
    <link rel="mask-icon" href="/favicon.svg" color="#6FD12E" />

    <!-- Fonts -->
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@600;700;800&display=swap"
    />

    <!-- Lottie Player web component -->
    <script src="https://unpkg.com/@lottiefiles/lottie-player@2.0.8/dist/lottie-player.js" defer></script>

    <!-- Centralized SEO + structured data -->
    <SEO {...seoProps} />

    <slot name="head" />
  </head>
  <body class="min-h-screen flex flex-col bg-white text-ink-900 dark:bg-ink-950 dark:text-ink-100 transition-colors">
    <a
      href="#main"
      class="sr-only focus:not-sr-only focus:fixed focus:top-2 focus:left-2 focus:z-[100] focus:rounded-md focus:bg-brand-600 focus:px-4 focus:py-2 focus:text-white"
    >
      Skip to main content
    </a>

    <Header />

    <main id="main" class="flex-1">
      <slot />
    </main>

    <Footer />

    <AccessibilityDrawer />

    <script>
      // Reveal-on-scroll for any element with data-reveal
      const io = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-visible');
              io.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.12, rootMargin: '0px 0px -40px 0px' }
      );
      document.querySelectorAll('[data-reveal]').forEach((el) => io.observe(el));
    </script>
  </body>
</html>
